package example;

import core.Game;
import core.GameEngine;
import core.GameRules;

public class Example extends GameEngine {
    public Example(GameRules rules, Game game) {
        super(rules, game);
    }
}
