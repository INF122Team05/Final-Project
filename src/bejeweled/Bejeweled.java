package bejeweled;

import core.Game;
import core.GameEngine;
import core.GameRules;

public class Bejeweled extends GameEngine {

    public Bejeweled(GameRules rules, Game game) {
        super(rules, game);
    }
}
